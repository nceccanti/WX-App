import React from 'react'
import locationService from '../features/locationService'

function Dashboard() {
    // const nav = await locationService.getRouteData()
    // console.log(nav)
  return (
    <div>
      dashboard
    </div>
      )
}

export default Dashboard